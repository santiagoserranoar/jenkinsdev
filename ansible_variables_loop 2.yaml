- hosts: localhost
  tasks:
   - name: busco carpetas
     find:
       paths: /var/lib/jenkins/workspace/BackendDev/Deploy/Publish/Development/
       recurse: no
       file_type: directory
     register: found_directories

- hosts: backendevinfra
  tasks:
   - name: Delete ApiGatewayPortal
     win_file:
        path: C:\ServiciosSA\DEV\{{ item }}
        state: absent
     with_items: "{{ found_directories.files }}"

#   - name: Copia ficheros
#     copy: 
#      src: /var/lib/jenkins/workspace/BackendDev/Deploy/Publish/Development/
#      dest: /tmp/
#     with_items: "{{ found_directories.files }}"


  